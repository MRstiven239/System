// Colors with a fixed meaning regardless of the active theme.
// Note: we now prefer using theme.expense/theme.income, but keep these for fallback.
export const DANGER = '#F87171';
export const SUCCESS = '#34D399';
